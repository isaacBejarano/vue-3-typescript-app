enum e_Storage {
	none = "none",
	granted = "granted",
	denied = "denied",
}

enum e_NavLinks {
	home = "/",
	form = "/form",
	pegi = "/pegi",
	tdd = "/tdd",
	lifecycle = "/lifecycle-hooks",
	vuex = "/vuex",
}

export { e_Storage, e_NavLinks };
