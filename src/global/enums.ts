enum e_Storage {
	none = "none",
	granted = "granted",
	denied = "denied",
}

enum e_NavLinks {
	home = "/",
	form = "/form",
	guard = "/guard",
	lifecycle = "/lifecycle-hooks",
	vuex = "/vuex",
	tdd = "/tdd",
}

export { e_Storage, e_NavLinks };
